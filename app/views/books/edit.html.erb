<% if @book.errors.any? %>
<div id=“error_explanation”>
  <h2><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
  <ul>
   <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
   <% end %>
  </ul>
<% end %>

<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
    <h4>Title</h4>
  　 <%= f.text_field :title %>
    <h4>Opinion</h4>
    <%= f.text_area :body %><br>

    <input type="submit" name="commit" value="Update Book" class="btn btn-primary col-xs-9" data-disable-with="update Book"><br />
<% end %>
<br><%= link_to "Show", book_path %>
|
<a href="/books">Back</a>
